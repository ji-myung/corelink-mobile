# corelink-mobile

강지명 (Corelink · Founder & Content Director) 모바일 명함.
Vite + TypeScript + 바닐라 DOM. GitHub Pages 배포.

## 수정 지점 규칙

이 프로젝트는 디자인 n차 수정을 전제로 만들었다. 무엇을 바꾸든 아래 표의 위치만 건드린다.

| 바꾸려는 것 | 수정할 파일 |
|---|---|
| 이름·연락처·서비스 목록·링크·포트폴리오 등 모든 콘텐츠 | `src/data/profile.ts` **한 곳만** |
| 색·간격·폰트 크기 같은 디자인 값 | `src/styles/tokens.css` (원시 값) |
| 종이 명함의 크기·해상도·평량 | `design/print/spec.mjs` 의 `CARD` |
| 다크/라이트 테마별 색 배정 | `src/styles/themes.css` (의미 토큰) |
| 특정 컴포넌트의 레이아웃 | `src/styles/components/<name>.css` |
| 섹션 구조·순서 | `src/main.ts` 의 렌더 순서, `src/components/<name>.ts` |

## 지켜야 할 제약

1. **컴포넌트 CSS는 의미 토큰만 참조한다.** `var(--color-accent)` 는 되고 `var(--gold-500)` 이나 `#D4A855` 같은 원시 값 직접 사용은 금지. 원시 토큰은 `themes.css` 안에서만 소비한다.
2. **콘텐츠를 컴포넌트에 하드코딩하지 않는다.** 화면 문구·연락처는 전부 `profile.ts` 에서 온다. 예외는 "복사됨" 같은 UI 라벨.
3. **선택 항목이 비면 UI에서 숨긴다.** `website`, `instagram`, `youtube`, `kakaoChannelId` 는 빈 문자열일 수 있다. 렌더 전에 확인하고, 섹션 전체가 비면 섹션도 그린다.
4. **테마 기본값은 dark.** 시스템 설정을 따라가지 않는다. 사용자가 토글해야 light 로 간다.
5. **vCard 는 정적 파일로 낸다.** iOS Safari 에서 JS Blob 다운로드는 저장 경로가 숨는다. `scripts/vite-plugin-vcard.ts` 가 `/card.vcf` 를 만들고, 링크는 `download` 속성 없이 `<a href>` 로 건다.
6. **터치 타겟 최소 44px, `env(safe-area-inset-*)` 존중.** 하단 액션바가 홈 인디케이터에 가리면 안 된다.

## 명령어

```
npm run dev        # 개발 서버
npm test           # vitest 1회 실행
npm run build      # 타입 검사 + 프로덕션 빌드
npm run typecheck  # 타입 검사만
```

## 참고

- 디자인 시안: `externals/명함 디자인 시안_260902.png` (블랙 + 골드)
- 전체 계획서: `docs/PLAN.md`

## 목업

`design/gen.mjs` 가 다크·라이트 아트보드를 **하나의 토큰 세트에서** 생성한다.
색을 바꾸려면 `gen.mjs` 상단의 `THEMES` 를, 구조를 바꾸려면 `page()` 를 고치고 `node design/gen.mjs` 를 다시 돌린다.
두 테마를 손으로 각각 고치지 않는다 — 어긋난다.

## 인쇄용 명함

`npm run print` 가 `design/print/gen-print.mjs` 로 종이 명함 앞/뒤를 찍어 `design/print/out/<빌드>/` 에 낸다
(4K 시안 PNG · **접수용 CMYK EPS** · 양면/낱장/재단선 PDF · 600dpi PNG · QR · `README_인쇄사양.md` · zip).
빌드 폴더는 기본 디자인이 `out/기본/`, 방송 이력 안은 `out/A_서비스_줄_캡션/` 처럼 태그가 붙는다. 서로 덮어쓰지 않는다.

규격은 `design/print/spec.mjs` 한 곳에서 온다. 두 벌로 나눠 뒀다 —
`CARD` 는 **내 명함 값**(재단·작업 크기, dpi, 평량)이라 마음대로 고쳐도 되고,
`SHOP` 은 **인쇄소 요구값**(안전영역 4mm, 잉크 상한 250%, 접수 형식, 박 별색 번호)이라
`docs/` 의 가이드를 다시 확인하지 않고는 건드리지 않는다. 도련은 작업−재단의 절반으로 계산되고,
가로·세로가 어긋나면 거기서 에러가 난다. 지금 값은 재단 86×52, 작업 88×54, 300dpi(=1039×638px), 250~300g.
크기를 바꾸면 요소가 어긋날 수 있는데 빌드 검증이 안전영역·겹침으로 잡는다. 위치는 편집기에서 옮긴다.
mm 좌표는 절대값이라 크기를 바꿔도 따라 움직이지 않는다 — 세로 크기를 바꿨으면 가운데 정렬을 다시 봐야 한다.

인쇄소에 넣는 것은 `02_인쇄소_접수용/` 뿐이다. 근거는 `docs/` 의 가이드 세 벌이다 —
`인쇄소_작업파일_가이드`(접수 형식), `공통_작업방법`(색·글꼴·효과·재단), `명함_작업방법`(사이즈·배열·저장).
요구는 이렇다: AI·EPS·PDF·JPG(ZIP) 중 하나 · CMYK(별색 금지) · **총 잉크량 250% 이하** ·
**K100 단색은 자동 오버프린트라 밑색이 비침** · 서체 아웃라인 · 효과는 300dpi 이상 CMYK 래스터화 ·
이미지 포함 · 재단선을 그려 넣지 않음 · 안전영역 재단선 안 4mm · 양면은 1p 앞 · 2p 뒤.

이 카드는 배경 그라데이션과 시안에서 떼어 온 반투명 그림이 전면을 덮어서 결국 전면 래스터화가 답이라,
600dpi CMYK 래스터를 **PDF(1p 앞·2p 뒤)와 EPS(면당 1장)** 두 벌로 낸다. 글자가 화소가 되니 아웃라인 요구도 같이 풀린다.
`RICH` 가 리치 블랙(C60 M40 Y40 K100 = 240%)을 어두운 곳에만 깔고, `TAC` 가 잉크 합 상한을 지킨다
(넘으면 K 는 두고 CMY 만 비례로 줄인다). 검정에 CMY 가 섞여 있어 K100 자동 오버프린트에도 걸리지 않는다.
QR 흰 판 안쪽만 K 단색으로 남긴다 — 0.32mm 모듈을 4도로 찍으면 판이 어긋나며 흐려진다.
판 위치는 렌더된 페이지에서 `.qr-panel` 을 재서 얻는다.

접수용 PDF 는 `pdfFromCmyk` 로 직접 쓴다. Chrome 이 낸 PDF 는 페이지 크기를 0.34mm 단위로 끊어
92×52mm 를 못 맞추고(91.9~92.05mm 가 같은 값) 색도 RGB 라 접수에 못 쓴다. `03_기타` 의 것이 그거다.
AI 네이티브 파일은 Illustrator 없이는 못 만든다.
콘텐츠는 `profile.ts`, 아이콘은 `src/icons.ts`, 색·크기는 `externals/` 시안을 픽셀 단위로 실측한 값이다(스크립트 주석에 근거를 적어 뒀다).
뒷면 우하단 골드 곡선은 시안 `명함 디자인 시안_뒤_260902.png` 에서 픽셀을 뽑아 얹으므로 그 파일이 있어야 돈다.
인쇄에만 있는 문구(태그라인, `&` 직함, 서비스 3개 선택)와 QR 규격은 스크립트 상단 `PRINT` 에 있다.
빌드마다 검증을 돌린다: QR 재판독(4K · 600dpi · **접수용 EPS 안의 화소**), 리치 블랙과 QR 모듈의 CMYK 값,
접수용 PDF 를 되읽어 페이지·크기·색공간·화소 대조, 총 잉크량과 K100 단색 화소 수,
안전영역(`SHOP.safe`), 요소끼리 겹침. 하나라도 어긋나면 종료 코드가 1 이다.
안전영역은 요소 상자가 아니라 렌더된 화소에서 **잉크 경계**를 찾아 잰다 — 글자 상자는 폰트 디센트만큼 커서
멀쩡한 배치도 걸린다(로고타입 표기가 상자 3.7mm / 잉크 4.4mm).
겹침은 잉크 상자로 보되 서로 품고 있는 짝(`.bcast` 안의 `.bcast-label`)은 건너뛴다.
잉크는 요소 상자 안에서만 찾으므로, 남의 잉크가 내 상자 안에 들어오면 내 것으로 잡힌다 —
그게 잡히면 상자가 실제로 부딪힌 것이라 배치를 고쳐야 한다(D안이 그랬다).

배치 값은 재단 86×52 기준으로 다시 잡았다(그 전에는 90×50 기준). 좌우 여백 6mm,
두 열 사이 세로선은 열 사이 정가운데, 목록 행 간격 5.15mm(잉크 사이 2.3mm),
서비스 목록과 연락처는 **첫 줄 윗선을 같은 높이(25.4mm)로** 맞춘다 — `--svc-y` 와 `--ct-y` 가 짝이다.
아이콘이 글자보다 커서 행의 잉크 높이는 글자 크기가 아니라 아이콘(`--svc-icon`/`--ct-icon`)이 정한다.

QR 은 `--qr-size` 로 줄일 수 있지만 모듈 한 칸이 작아지면 인쇄물에서 안 읽힌다.
빌드가 모듈 실측 치수를 찍고 `QR_MIN_MODULE`(0.25mm) 미만이면 경고한다 — 막지는 않는다(줄이는 건 디자인 선택).
사양서에도 같은 경고가 자동으로 붙는다. 파일 안에서의 판독 성공은 인쇄물에서 읽힌다는 뜻이 아니다.
방송 협업 이력(`PRINT.broadcasts`)을 어디에 넣을지는 `PRINT.variant` 로 고른다.
`npm run print:samples` 가 배치안을 전부 4K 로 `design/print/samples/` 에 뽑아 준다 — 고른 키로 `npm run print -- --variant=caption` 처럼 패키지를 뽑는다.
앞면 안과 뒷면 안은 `--variant=caption+front` 처럼 `+` 로 겹친다. 같은 면에 두 안을 겹치면 에러로 막는다.
H~N 은 `externals/나는솔로_솔로지옥.png` 를 따른 로고타입 표기이며 모두 우하단 골드 곡선을 지운다.
조판 값 `--mark-*` 은 그 사진을 수평으로 편 뒤 낱덩어리 잉크 치수를 재고 값을 탐색해 맞춘 것이다(오차 최대 2.2%p). 임의로 바꾸면 참고본과 멀어진다.
좌우 정렬(오른쪽·가운데·연락처 열)만 `MARK` 에서 고르고, 나머지는 변수다.
변형 빌드는 파일명·재단선 라벨에 태그(A~E)가 붙고 사양서 끝에 추가 표기 항목이 생긴다.
`fonts/` 와 `out/` 은 git 에 넣지 않는다. 폰트는 없으면 자동으로 받는다.

### 디자인 값을 손으로 고치기

`npm run print:edit` → `http://localhost:4321` 에 앞·뒤가 뜬다. 인쇄 렌더와 **같은 HTML·CSS** 다.
고치는 도구는 브라우저 개발자도구다 — Elements 에서 `<html>` 을 고르면 Styles 에 `:root` 의 변수 125개가 전부 보인다.
숫자는 방향키로 0.1(Shift 는 1)씩 움직이고 색은 스와치로 고른다. [저장] 을 누르면 **기본값과 다른 것만**
`design/print/layout.json` 에 남고, 그다음 `npm run print` 가 그 값으로 패키지를 뽑는다.

값의 원본은 `gen-print.mjs` 의 `VARS` 다. 여기 없는 값은 화면에서 고쳐도 저장되지 않으므로, 만지고 싶은 값이 생기면
리터럴을 `VARS` 로 빼고 컴포넌트를 `var()` 로 바꾼다 — **컴포넌트에 숫자·색을 직접 적지 않는다.**
저장은 `:root` 규칙의 원문을 읽는다(계산값이 아니다). 그래야 `var(--c-accent)` 같은 참조가 색 리터럴로 굳지 않는다.
`layout.json` 은 안(variant) 과 무관한 하나의 파일이라 모든 빌드에 함께 적용된다.
값을 바꿔 뽑은 패키지는 사양서 끝에 "손으로 고친 디자인 값" 절이 붙고, 6절 폰트 줄은 실제 쓴 폰트로 채워진다.

`VARS` 를 고칠 때는 **기본값 그대로 뽑은 결과가 이전과 바이트 단위로 같아야 한다**. 4K PNG 와 600dpi PNG 를
`sha256sum` 으로 견주는 것으로 확인한다 — 리터럴을 변수로 옮기는 작업에서 값이 새는지 이걸로 잡는다.

## 값을 채우면 나타나는 것

`profile.ts` 에서 아래 값이 빈 문자열이면 화면에 그리지 않는다. 채우면 자동으로 나타난다.

| 값 | 채우면 |
|---|---|
| `website` | 카드 안 연락처에 홈페이지 줄이 생긴다 |
| `kakaoChannelId` (`_XXXX`) | 하단 액션바가 2칸에서 3칸으로 늘고 카카오 상담이 생긴다 |
| `instagram` / `youtube` | 채널 섹션이 생긴다. 하나만 넣으면 하나만 나온다 |
| `portfolio` | 포트폴리오 섹션이 생긴다. 항목 수에 따라 뒤 카드가 쌓인다 |
| `bio` | 직함 아래 한 줄 소개가 생긴다 |

QR 은 `siteUrl` 을 담아 빌드 시점에 굽는다. 주소를 바꾸면 QR 도 따라 바뀐다.
